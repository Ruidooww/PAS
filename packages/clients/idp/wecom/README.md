# WeCom IdP client

Implements the WeCom OAuth scaffold: authorize URL, `gettoken`, `auth/getuserinfo`, and `auth/getuserdetail`, with fetch injection for unit tests. Real callback E2E remains skipped until dev credentials are available.